package com.yclin.achieveapp.ui.theme

import androidx.compose.ui.graphics.Color

// 主色调
val Primary = Color(0xFF4CAF50) // 绿色作为主色
val PrimaryVariant = Color(0xFF2E7D32) // 深绿色
val Secondary = Color(0xFF03A9F4) // 浅蓝色作为次要色
val SecondaryVariant = Color(0xFF0288D1) // 深蓝色

// 背景色
val Background = Color(0xFFF5F5F5) // 浅灰色背景
val Surface = Color(0xFFFFFFFF) // 白色表面

// 文本颜色
val OnPrimary = Color(0xFFFFFFFF) // 主色上的文字颜色 (白色)
val OnSecondary = Color(0xFFFFFFFF) // 次要色上的文字颜色 (白色)
val OnBackground = Color(0xFF212121) // 背景上的文字颜色 (深灰色)
val OnSurface = Color(0xFF212121) // 表面上的文字颜色 (深灰色)

// 状态颜色
val Error = Color(0xFFB00020) // 错误颜色
val OnError = Color(0xFFFFFFFF) // 错误颜色上的文字颜色

// 任务优先级颜色
val LowPriority = Color(0xFF8BC34A) // 低优先级 (浅绿色)
val MediumPriority = Color(0xFFFFC107) // 中优先级 (黄色)
val HighPriority = Color(0xFFF44336) // 高优先级 (红色)

// 习惯相关颜色
val HabitCompleted = Color(0xFF4CAF50) // 已完成习惯 (绿色)
val HabitMissed = Color(0xFFF44336) // 未完成习惯 (红色)
val HabitNeutral = Color(0xFF9E9E9E) // 中性状态 (灰色)